# Secret-Zero - Local environment template
#
# Usage:
#   cp env.example .env.local
#   # then fill in the values below
#
# Notes:
# - Never commit real secrets or private keys.
# - Keys should be PEM strings stored as a single line with literal "\n" separators.
#   When wrapped in double quotes, dotenv/Next.js will convert "\n" into real newlines.
#
# ---- Stytch (B2B) ----
STYTCH_PROJECT_ID=
STYTCH_SECRET=
NEXT_PUBLIC_STYTCH_PUBLIC_TOKEN=

# Optional: used to verify Stytch webhook requests at /api/webhooks/stytch
STYTCH_WEBHOOK_SECRET=

# ---- Infisical ----
INFISICAL_CLIENT_ID=
INFISICAL_CLIENT_SECRET=
INFISICAL_PROJECT_ID=

# Optional (default: https://app.infisical.com)
INFISICAL_SITE_URL=https://app.infisical.com

# Optional (default: prod)
INFISICAL_ENVIRONMENT=prod

# ---- RSA Encryption Keys (PEM) ----
# Generate:
#   openssl genpkey -algorithm RSA -pkeyopt rsa_keygen_bits:2048 -out private.pem
#   openssl pkey -in private.pem -pubout -out public.pem
#
# Convert to single line (adds "\n" at the end of each line):
#   awk '{printf "%s\\\\n", $0}' private.pem
#   awk '{printf "%s\\\\n", $0}' public.pem
SERVER_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\\n...\\n-----END PRIVATE KEY-----\\n"
NEXT_PUBLIC_SERVER_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----\\n...\\n-----END PUBLIC KEY-----\\n"

# ---- App ----
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ---- Footer configuration (optional - defaults provided) ----
# Logo path (default: /logo.svg). Supports `/...` files from `public/` or an `https://...` URL. Set to empty to hide the logo.
NEXT_PUBLIC_FOOTER_LOGO_PATH=/logo.svg

# URL for logo click (default: NEXT_PUBLIC_APP_URL or /). Set to empty to disable the link.
NEXT_PUBLIC_FOOTER_LOGO_URL=

# Privacy policy URL (default: NEXT_PUBLIC_APP_URL or /). Set to empty to disable the link.
NEXT_PUBLIC_FOOTER_PRIVACY_URL=

# Company name in footer (default: "Secret Zero by Grzegorz Zawłodzki")
NEXT_PUBLIC_FOOTER_COMPANY_NAME=Secret Zero by Grzegorz Zawłodzki

# ---- Optional notifications ----
# Receives metadata-only notifications (never secret values).
ADMIN_WEBHOOK_URL=

# If set, outgoing webhook requests include X-Webhook-Signature (HMAC SHA-256 hex).
WEBHOOK_SECRET=

# ---- Optional test helpers (scripts/) ----
TEST_EMAIL=
